<form method="post" action="{{ path('app_chevaux_delete', {'id': chevaux.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ chevaux.id) }}">
    <button class="btn">Delete</button>
</form>
